inicio() {
    inteiro numeros[3];
    
    numeros[0] = 10;
    numeros[1] = 20;
    numeros[2] = 30;
    
    inteiro soma = numeros[0] + numeros[1] + numeros[2];
    
    exibir("A soma dos elementos do vetor e:");
    exibir(soma);
}