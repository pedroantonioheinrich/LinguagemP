inicio() {
    inteiro lista[3];
    
    lista[0] = 10;
    lista[1] = 20;
    lista[2] = 30;
    
    exibir("Valor na posicao 1:");
    exibir(lista[1]);
    
    inteiro soma;
    soma = lista[0] + lista[2];
    exibir("Soma do primeiro com o ultimo:");
    exibir(soma);
}